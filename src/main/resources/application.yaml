server:
  port: 8080
  servlet:
    context-path: /miso


spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3307/misodb?serverTimezone=Asia/Shanghai
    username: root
    password: root
  mvc:
    view:
      suffix: .jsp
      prefix: /

  redis:
    database: 0
    host: localhost
    port: 6379
    password:

mybatis-plus:
  global-config:
    db-config:
      id-type: auto

logging:
  file:
    path: D://misolog//miso_demo_log

  level:
    com.chixing: debug



